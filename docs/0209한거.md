# 2026-02-09 작업 내역

## 이전 세션에서 이어서 작업한 내용 (0204 이후)

### 1. 찜하기(즐겨찾기) 로직 수정

**문제**: 찜 버튼 누르면 바로 풀리고, 다른 축제도 같이 눌림
**원인**: `CODENAME`은 고유 ID가 아니라 카테고리 (클래식, 무용, 콘서트 등). 같은 CODENAME을 가진 축제들이 동시에 찜됨
**해결**: `festival.CODENAME` → `festival.TITLE`로 변경

| 파일 | 변경 |
|------|------|
| `FestivalCard.tsx` | `onToggleFavorite(festival.TITLE)`, console.log 제거 |
| `FestivalListContainer.tsx` | `isFavorite={favoriteFestivals.has(festival.TITLE)}` |
| `FestivalContext.tsx` | favorites 필터에서 TITLE 사용 |
| `FilterContext.tsx` | console.log 제거 |
| `LanguageButton.tsx` | console.log 제거 |

---

### 2. SVG 지도 마커/뱃지 위치 보정

**문제**: 마커 핀과 구 중심점 원의 위치가 안 맞음
**해결**:
- 뱃지 오프셋: `(x+30, y-30)` → `(x+14, y-32)`
- 마커 오프셋: `translate(x, y+15)` → `translate(x, y)`
- `guData.ts`의 25개 구 중심 좌표도 수동 보정

| 파일 | 변경 |
|------|------|
| `svgMapUtils.ts` | 뱃지/마커 좌표 오프셋 수정 |
| `useSvgMapOverlays.ts` | hover 핸들러 좌표 수정 |
| `guData.ts` | 25개 구 center 좌표 수동 조정 |

---

### 3. 네이버 지도 마커 간헐적 미표시

**문제**: 네이버 지도에서 마커가 나타날 때도 있고 안 나타날 때도 있음
**원인**: `initMap`이 `festival` 의존성을 가져서 축제 데이터 변경마다 지도가 재생성됨
**해결**:
- `initMap`에서 `festival` 의존성 제거 → 지도는 1회만 초기화
- `naver.maps.Event.addListenerOnce(map, 'idle', ...)` 추가 → 지도 렌더링 완료 후 마커 추가

| 파일 | 변경 |
|------|------|
| `useNaverMap.ts` | festival 의존성 제거, idle 이벤트 리스너 추가 |

---

### 4. 계절별 파티클 이펙트 구현

`@tsparticles/react` + `@tsparticles/slim` 사용

**계절별 이모지 파티클:**
| 계절 | 이모지 | 방향 |
|------|--------|------|
| 봄 | 🌸, 🏵️ | 위→아래 |
| 여름 | ⭐ (별 도형) | 랜덤 |
| 가을 | 🍂, 🍁, 🍃 | 위→아래 |
| 겨울 | ❄, ❆ | 위→아래 |

**해결한 이슈들:**
- **속도 가속 문제**: `wobble` + `drift`가 방향성 이동과 합쳐져 매 프레임 속도 누적 → wobble/drift 제거, `random: true` 사용
- **컨테이너 누수**: `key={season}` 변경 시 tsparticles 엔진이 이전 컨테이너 미해제 → `key` 제거, `container.destroy()` cleanup
- **겹침 문제**: 파티클과 콘텐츠가 형제 관계라 겹치지 않음 → `BackgroundContainer` 패턴으로 분리

| 파일 | 변경 |
|------|------|
| `SeasonalParticles.tsx` | 새 파일 - 계절별 파티클 컴포넌트 |
| `BackgroundContainer.tsx` | 새 파일 - 배경(이미지+그라데이션+파티클) 통합 컨테이너 |
| `App.tsx` | 인라인 배경 → BackgroundContainer로 교체 |

---

### 5. 라우터 정리

**문제**: NotFound 처리가 3곳에 중복
**해결**: `main.tsx`의 와일드카드 `*` 라우트 제거 (dead code)
- React Router `errorElement` 유지 (라우터 에러 fallback)
- `viewMode === "notfound"` 유지 (실제 404 핸들러 via useUrlSync)

---

### 6. 마커 클릭 시 URL 미반영

**문제**: SVG 지도에서 마커 클릭 → 상세페이지 이동되지만 URL이 안 바뀜
**원인**: `navigateToDetail`이 `selectedFestival`만 설정하고 `selectedDistrict`를 설정 안 함. `useUrlSync`는 `selectedDistrict` 필요
**해결**: `handleFestivalSelect`에 `filter.setSelectedDistrict(festival.GUNAME)` 추가

| 파일 | 변경 |
|------|------|
| `FestivalContext.tsx` | handleFestivalSelect에서 setSelectedDistrict 호출 |

---

## 02-09 작업 내역

### 7. 모바일 반응형 레이아웃 구현

**목표**: md(768px) 미만에서 탭 전환 기반 모바일 레이아웃

**설계 결정:**
- `useIsMobile` 훅 불필요 → Tailwind 반응형 클래스 + `mobileTab` state만으로 해결
- SVG viewBox 내부 좌표라 지도 축소 시 마커 자동 비례 → 재배치 불필요
- 해/달(TimetoScrolling)은 `%` 기반 위치라 자동 비례 축소 → 그대로 유지

**모바일 레이아웃:**
```
┌──────────────┐
│ 로고  🔍  👤 │  ← 간소화 헤더
├──────────────┤
│[지도] [목록] │  ← 탭 바 (md:hidden)
├──────────────┤
│              │
│ 지도 or 목록  │  ← 탭 전환 (풀 너비)
│              │
└──────────────┘
```

**구 선택 시 자동 목록 탭 전환:**
```typescript
useEffect(() => {
  if (selectedDistrict) setMobileTab("list");
}, [selectedDistrict]);
```

**상세 페이지 (모바일):** CSS order로 순서 변경
- `order-1 md:order-2` (Right: 제목/이미지 먼저)
- `order-2 md:order-1` (Left: 상세/리뷰 뒤에)

**새 파일:**

| 파일 | 설명 |
|------|------|
| `src/components/MobileTabBar.tsx` | 지도/목록 탭 바 (md:hidden) |

**수정 파일:**

| 파일 | 변경 |
|------|------|
| `App.tsx` | mobileTab state, MobileTabBar, className 분기, flex-col→md:flex-row |
| `HeaderContainer.tsx` | 모바일 헤더 추가 (로고+검색+로그인), lg:→md: 통일 |
| `LeftSectionContainer.tsx` | w-full md:w-[40%], h-auto md:h-[calc], rounded-none md:rounded |
| `RightSectionContainer.tsx` | w-full md:w-[40%], p-4 md:p-12, rounded-none md:rounded |
| `SeoulMapContainer.tsx` | w-full md:w-[60%], max-h-[60vh] md:max-h-[834px] |
| `SearchInput.tsx` | w-full md:w-[280px] |
| `GridButtonGroup.tsx` | grid-cols-4 md:grid-cols-5 |
| `CardLayout.tsx` | h-auto min-h-[300px] md:h-[394px], rounded-[20px] md:rounded-tr-[40px] |
| `FooterContainer.tsx` | lg:→md: 브레이크포인트 통일 |

---

### 8. 모바일 레이아웃 버그 4가지 수정

#### 8-1. 상세페이지 클릭 시 지도로 돌아감

**원인**: `handleFestivalSelect`에서 `setSelectedFestival(festival)` 후 `setSelectedDistrict(GUNAME)` 호출 → `setSelectedDistrict`가 내부적으로 `selectedFestival`을 `null`로 리셋 → URL sync가 `/`로 돌아감
**해결**: 호출 순서 반전 (district 먼저, festival 나중 = React 배치 업데이트에서 최종값 유지)

| 파일 | 변경 |
|------|------|
| `FestivalContext.tsx` | handleFestivalSelect 호출 순서 반전 (2줄 swap) |

#### 8-2. 모바일에서 계절버튼 안보임

**원인**: SeasonButton이 CardLayout 안에서 지도(max-h-[60vh]) 아래에 밀려 안 보임
**해결**: SeasonButton에 `order-first md:order-0` CSS 적용 → 모바일에서 지도 위에 표시, 데스크톱은 기존과 동일

| 파일 | 변경 |
|------|------|
| `LeftContent.tsx` | SeasonButton에 `order-first md:order-0` div wrapper 추가 |

#### 8-3. 모바일 리스트에서 클릭 시 바로 상세로 이동

**원인**: FestivalCard onClick이 `setSelectedFestival`만 호출 → 모바일 탭 분리로 프리뷰 안 보임
**해결**: `window.matchMedia('(max-width: 767px)')` 체크 → 모바일: `navigateToDetail`, 데스크톱: `setSelectedFestival` (기존 동작 유지)

| 파일 | 변경 |
|------|------|
| `FestivalListContainer.tsx` | navigateToDetail 추가, 클릭 핸들러 matchMedia 분기 |

#### 8-4. 모바일 상세에서 Right→Left 순서

**상태**: App.tsx에 `order-1 md:order-2`, `order-2 md:order-1` 이미 적용됨 → 별도 수정 없이 정상 동작

---

## 남은 작업

- [ ] 모바일 레이아웃 실제 테스트 (375px, 390px 뷰포트)
- [ ] 파티클 속도 안정성 최종 확인
- [ ] 빌드 확인 (`pnpm build`)
- [ ] 배포 후 모바일 브라우저 테스트