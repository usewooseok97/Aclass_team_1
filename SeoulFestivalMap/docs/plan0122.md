# 축제 상세페이지 구현 계획 (plan0122)

## 개요
LeftContent.tsx에서 축제 이미지나 "상세 페이지 >" 링크 클릭 시 외부 URL 대신 내부 상세 페이지로 이동하도록 변경

## 디자인 참고
- `src/assets/상세페이지상부.png` - 헤더, 이미지 갤러리, 기본 정보
- `src/assets/상세페이지중부.png` - 주변 맛집, 상세 정보, 버튼
- `src/assets/상세페이지하부.png` - 오시는 길 (지도)

---

## 1. 새로 만들 컴포넌트

### Atoms (기본 UI)
| 컴포넌트 | 파일 경로 | 설명 |
|---------|----------|------|
| `Badge` | `src/atoms/Badge.tsx` | 태그 뱃지 (축제/지역구 등) |
| `StarRating` | `src/atoms/StarRating.tsx` | 별점 표시 (기존 코드 추출) |
| `IconText` | `src/atoms/IconText.tsx` | 아이콘 + 라벨 + 값 레이아웃 |

### Components (복합 UI)
| 컴포넌트 | 파일 경로 | 설명 |
|---------|----------|------|
| `RestaurantCard` | `src/components/RestaurantCard.tsx` | 맛집 카드 (거리/평점/카테고리) |

### Pages (페이지)
| 컴포넌트 | 파일 경로 | 설명 |
|---------|----------|------|
| `FestivalDetailPage` | `src/pages/FestivalDetailPage/index.tsx` | 상세 페이지 메인 |
| `FestivalDetailHeader` | `src/pages/FestivalDetailPage/FestivalDetailHeader.tsx` | 뒤로가기 + 태그 + 제목 + 평점 |
| `FestivalImageGallery` | `src/pages/FestivalDetailPage/FestivalImageGallery.tsx` | 이미지 갤러리 |
| `FestivalBasicInfo` | `src/pages/FestivalDetailPage/FestivalBasicInfo.tsx` | 기본 정보 섹션 |
| `FestivalDescription` | `src/pages/FestivalDetailPage/FestivalDescription.tsx` | 축제 소개 |
| `NearbyRestaurants` | `src/pages/FestivalDetailPage/NearbyRestaurants.tsx` | 주변 맛집 섹션 |
| `FestivalDetailInfo` | `src/pages/FestivalDetailPage/FestivalDetailInfo.tsx` | 상세 정보 (주최/입장료 등) |
| `FestivalActionButtons` | `src/pages/FestivalDetailPage/FestivalActionButtons.tsx` | 예약/공유 버튼 |
| `FestivalLocationMap` | `src/pages/FestivalDetailPage/FestivalLocationMap.tsx` | 오시는 길 (지도) |

---

## 2. 기존 컴포넌트 재사용

| 컴포넌트 | 용도 |
|---------|------|
| `BackButton` | 목록으로 돌아가기 버튼 |
| `CardLayout` | 각 섹션 카드 컨테이너 |
| `TitleText` | 축제 제목 |
| `ContentsText` | 설명 텍스트 |

---

## 3. Lucide-React 아이콘 매핑

| 용도 | 아이콘 | import |
|-----|-------|--------|
| 위치 | 📍 | `MapPin` |
| 기간 | 📅 | `Calendar` |
| 대상 | 👥 | `Users` |
| 맛집 | 🍴 | `Utensils` |
| 주최 | 🏛️ | `Building2` |
| 입장료 | 🎫 | `Ticket` |
| 주차 | 🅿️ | `ParkingCircle` |
| 대중교통 | 🚇 | `Train` |
| 문의 | 📞 | `Phone` |
| 별점 | ⭐ | `Star` |
| 공유 | 📤 | `Share2` |
| 지도 | 🗺️ | `Map` |

---

## 4. Context 수정 (페이지 전환)

### types/festival.ts 추가
```typescript
export type ViewMode = 'map' | 'list' | 'detail';
```

### FestivalContextValue 확장
```typescript
viewMode: ViewMode;
setViewMode: (mode: ViewMode) => void;
```

### FestivalContext.tsx 수정
- `viewMode` state 추가 (기본값: 'map')
- `setViewMode` 함수 추가
- `setSelectedFestival` 호출 시 자동으로 viewMode를 'detail'로 변경하는 헬퍼 함수 추가

---

## 5. 수정할 기존 파일

| 파일 | 수정 내용 |
|-----|----------|
| `src/types/festival.ts` | ViewMode 타입 추가 |
| `src/contexts/FestivalContext.tsx` | viewMode 상태 및 함수 추가 |
| `src/App.tsx` | viewMode에 따른 조건부 렌더링 추가 |
| `src/containers/LeftContent.tsx` | 외부 링크 → 내부 네비게이션으로 변경 |

---

## 6. 구현 순서

### Phase 1: 기반 작업
1. `types/festival.ts` - ViewMode 타입 추가
2. `contexts/FestivalContext.tsx` - viewMode 상태 추가
3. `atoms/Badge.tsx` 생성
4. `atoms/StarRating.tsx` 생성
5. `atoms/IconText.tsx` 생성

### Phase 2: 상세 페이지 컴포넌트
6. `pages/FestivalDetailPage/FestivalDetailHeader.tsx`
7. `pages/FestivalDetailPage/FestivalImageGallery.tsx`
8. `pages/FestivalDetailPage/FestivalBasicInfo.tsx`
9. `pages/FestivalDetailPage/FestivalDescription.tsx`
10. `components/RestaurantCard.tsx`
11. `pages/FestivalDetailPage/NearbyRestaurants.tsx`
12. `pages/FestivalDetailPage/FestivalDetailInfo.tsx`
13. `pages/FestivalDetailPage/FestivalActionButtons.tsx`
14. `pages/FestivalDetailPage/FestivalLocationMap.tsx`
15. `pages/FestivalDetailPage/index.tsx`

### Phase 3: 통합
16. `App.tsx` - 조건부 렌더링 추가
17. `LeftContent.tsx` - 링크 → 내비게이션 변경

---

## 7. 데이터 매핑

| UI 요소 | 데이터 소스 |
|--------|------------|
| 제목 | `festival.TITLE` |
| 위치 | `festival.PLACE` |
| 기간 | `festival.DATE` |
| 대상 | `festival.USE_TRGT` |
| 설명 | `festival.PROGRAM` |
| 주최 | `festival.ORG_NAME` |
| 입장료 | `festival.IS_FREE` |
| 평점 | `festival.buzz_score / 20` |
| 지역구 태그 | `festival.GUNAME` |
| 이미지 | `festival.MAIN_IMG` |
| 주변 맛집 | `nearbyPlaces[]` |

### 누락 데이터 → 플레이스홀더 표시
| 항목 | 플레이스홀더 값 |
|-----|---------------|
| 주차 정보 | "정보 없음" |
| 대중교통 정보 | "정보 없음" |
| 수용 인원 | "정보 없음" |
| 추가 이미지 | 메인 이미지만 표시 (갤러리는 단일 이미지) |
| 맛집 거리 | 표시하지 않음 (데이터 없음) |
| 문의 전화 | place.telephone 사용 또는 "정보 없음" |

---

## 8. 검증 방법

1. `npm run dev`로 개발 서버 실행
2. 메인 페이지에서 지역구 선택 → 축제 목록 확인
3. 축제 카드 클릭 → LeftContent에서 상세 미리보기 확인
4. 이미지 또는 "상세 페이지" 클릭 → 상세 페이지 이동 확인
5. 뒤로가기 버튼 → 목록으로 복귀 확인
6. 주변 맛집 목록 표시 확인
7. 시간대별 테마 적용 확인 (CSS 변수)

---

## 9. 페이지 레이아웃 구조

```
FestivalDetailPage
├── FestivalDetailHeader (뒤로가기, 태그, 제목, 평점)
├── CardLayout
│   ├── FestivalImageGallery
│   └── FestivalBasicInfo (위치, 기간, 대상)
├── CardLayout
│   └── FestivalDescription (축제 소개)
├── CardLayout
│   └── NearbyRestaurants (주변 맛집)
├── CardLayout
│   ├── FestivalDetailInfo (상세 정보)
│   └── FestivalActionButtons (예약, 공유)
└── CardLayout
    └── FestivalLocationMap (오시는 길)
```
