# 2026-01-20 수정 사항

## 1. 이모지를 아이콘으로 교체

### 변경 파일
- `src/containers/LeftContent.tsx`
- `src/components/FestivalCard.tsx`

### 변경 내용
| 이모지 | 아이콘 (lucide-react) |
|--------|----------------------|
| 📍 | `<MapPin />` |
| 📅 | `<Calendar />` |
| 🍽 | `<Utensils />` |
| 🎪 | `<PartyPopper />` |
| ★ | `<Star />` |

---

## 2. 다이내믹 애니메이션 헤더 구현

### 변경 파일
- `src/hooks/useTimePhase.ts`
- `src/components/TimetoScrolling.tsx`
- `src/containers/HeaderContainer.tsx`
- `src/App.tsx`

### 기능
- 실시간 시간에 따라 배경색 변화 (4단계: morning/day/sunset/night)
- 해와 달이 원형 궤도로 회전 (180° 반대편 위치)
- 지평선(60%) 기준으로 해/달 visibility 자동 처리

### 시간대별 그라데이션
| 시간대 | 시간 | 색상 |
|--------|------|------|
| Morning | 06:00~09:00 | 분홍 → 주황 → 노랑 |
| Day | 09:00~16:00 | 하늘색 그라데이션 |
| Sunset | 16:00~18:00 | 주황 → 분홍 → 보라 |
| Night | 18:00~06:00 | 딥 네이비/보라 |

### 원형 궤도 계산 (useTimePhase.ts)
```typescript
const calculateCircularPosition = (hour: number, minute: number) => {
  const timeInHours = hour + minute / 60;
  const sunAngleDeg = (timeInHours / 24) * 360 - 90;  // 12시에 정점
  const sunAngle = sunAngleDeg * (Math.PI / 180);
  const moonAngle = sunAngle + Math.PI;  // 해와 180° 반대

  const centerX = 50;   // 원의 중심 X
  const centerY = 60;   // 지평선 위치
  const radius = 35;    // 궤도 반지름

  return {
    sun: { x: centerX + radius * Math.cos(sunAngle), y: centerY - radius * Math.sin(sunAngle) },
    moon: { x: centerX + radius * Math.cos(moonAngle), y: centerY - radius * Math.sin(moonAngle) }
  };
};
```

---

## 3. 시간대별 색상 테마 적용 (CSS Variables)

### 변경 파일
- `src/App.tsx` - CSS 변수 설정
- `src/atoms/CardLayout.tsx`
- `src/atoms/TitleText.tsx`
- `src/atoms/FooterText.tsx`
- `src/atoms/ContentsText.tsx`
- `src/containers/GuButtonContainer.tsx`
- `src/components/FestivalCard.tsx`
- `src/components/WeatherLocation.tsx`
- `src/components/SearchInput.tsx`
- `src/containers/LeftContent.tsx`

### CSS 변수 정의 (App.tsx)
```typescript
const getThemeColors = (phase: string): React.CSSProperties => ({
  '--text-primary': phase === 'night' ? '#f1f5f9' : phase === 'sunset' ? '#581c87' : '#1e293b',
  '--text-secondary': phase === 'night' ? '#94a3b8' : phase === 'sunset' ? '#a855f7' : '#64748b',
  '--btn-primary': phase === 'morning' ? '#f97316' : phase === 'night' ? '#818cf8' : phase === 'sunset' ? '#7c3aed' : '#6750A4',
  '--btn-hover': phase === 'morning' ? '#ea580c' : phase === 'night' ? '#6366f1' : phase === 'sunset' ? '#6d28d9' : '#5b3f9a',
  '--card-bg': phase === 'night' ? 'rgba(30,27,75,0.7)' : 'rgba(255,255,255,0.85)',
  '--card-border': phase === 'morning' ? '#fdba74' : phase === 'night' ? '#4c1d95' : phase === 'sunset' ? '#c084fc' : '#93c5fd',
});
```

### 시간대별 색상 팔레트
| 요소 | Morning | Day | Sunset | Night |
|------|---------|-----|--------|-------|
| 텍스트 (주) | #1e293b | #1e293b | #581c87 | #f1f5f9 |
| 텍스트 (보조) | #64748b | #64748b | #a855f7 | #94a3b8 |
| 버튼 배경 | #f97316 | #6750A4 | #7c3aed | #818cf8 |
| 카드 배경 | rgba(255,255,255,0.85) | rgba(255,255,255,0.85) | rgba(255,255,255,0.85) | rgba(30,27,75,0.7) |
| 카드 테두리 | #fdba74 | #93c5fd | #c084fc | #4c1d95 |

---

## 테스트 방법

1. `pnpm dev` 실행
2. `src/hooks/useTimePhase.ts` 82번 줄 주석 해제하여 시간 수동 변경:
   ```typescript
   const now = new Date(1995, 11, 17, 6, 0, 0); // 테스트용: 시간 변경
   ```
3. 시간 값 변경하여 각 phase 확인:
   - 6시: morning (해 떠오름)
   - 12시: day (해 정점)
   - 17시: sunset (해 지는 중)
   - 0시: night (달 정점)
